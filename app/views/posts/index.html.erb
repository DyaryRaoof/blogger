<%= render "/users/single_user", u: @user%>
<% @posts.each do |p| %>
    <%= render '/posts/single_post', p:p %>
    <%= render 'list_of_comments', comments: Comment.where(author_id: p.author_id, post_id: p.id),user: @user %>
<% end %>
<div class="center-button">
    <button>Pagination</button>
</div>